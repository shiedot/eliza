{
    "buildCommand": "bun install --ignore-scripts && bun run build:client",
    "outputDirectory": "packages/client/dist",
    "installCommand": "bun install --ignore-scripts",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}