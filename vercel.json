{
  "buildCommand": "bun install && cd packages/client && bun run build",
  "outputDirectory": "packages/client/dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
